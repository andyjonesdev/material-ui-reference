import styled from "styled-components"

const NavContainer = styled.div`
    padding-top: 1vh;
    margin-bottom: 4vh;
`

const Nav = () => {
    return(
        <NavContainer>
            <h1>Andy's Material UI Reference</h1>
        </NavContainer>
    )
}

export default Nav
